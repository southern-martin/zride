version: '3.8'

services:
  # Database
  postgres:
    image: postgres:15-alpine
    container_name: zride-postgres
    environment:
      POSTGRES_DB: zride
      POSTGRES_USER: zride_user
      POSTGRES_PASSWORD: zride_password
    ports:
      - "5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data
      - ./scripts/init.sql:/docker-entrypoint-initdb.d/init.sql
    networks:
      - zride-network

  # Redis for caching and sessions
  redis:
    image: redis:7-alpine
    container_name: zride-redis
    ports:
      - "6379:6379"
    networks:
      - zride-network

  # Auth Service
  auth-service:
    build:
      context: ./services/auth-service
      dockerfile: Dockerfile
    container_name: zride-auth-service
    ports:
      - "8081:8081"
    environment:
      - PORT=8081
      - DB_HOST=postgres
      - DB_PORT=5432
      - DB_USER=zride_user
      - DB_PASSWORD=zride_password
      - DB_NAME=zride_auth
      - JWT_SECRET=your_jwt_secret_key_here_make_it_long_and_secure
      - ZALO_APP_ID=your_zalo_app_id
      - ZALO_APP_SECRET=your_zalo_app_secret
      - REDIS_HOST=redis
      - REDIS_PORT=6379
    depends_on:
      - postgres
      - redis
    networks:
      - zride-network

  # User Service
  user-service:
    build:
      context: ./services/user-service
      dockerfile: Dockerfile
    container_name: zride-user-service
    ports:
      - "8082:8082"
    environment:
      - PORT=8082
      - DB_HOST=postgres
      - DB_PORT=5432
      - DB_USER=zride_user
      - DB_PASSWORD=zride_password
      - DB_NAME=zride_users
      - JWT_SECRET=your_jwt_secret_key_here_make_it_long_and_secure
      - AUTH_SERVICE_URL=http://auth-service:8081
      - UPLOAD_MAX_SIZE=10
      - UPLOAD_PATH=./uploads
    depends_on:
      - postgres
      - auth-service
    volumes:
      - user_uploads:/app/uploads
    networks:
      - zride-network

  # Trip Service
  trip-service:
    build:
      context: ./services/trip-service
      dockerfile: Dockerfile
    container_name: zride-trip-service
    ports:
      - "8083:8083"
    environment:
      - PORT=8083
      - DB_HOST=postgres
      - DB_PORT=5432
      - DB_USER=zride_user
      - DB_PASSWORD=zride_password
      - DB_NAME=zride_trips
      - JWT_SECRET=your_jwt_secret_key_here_make_it_long_and_secure
      - AUTH_SERVICE_URL=http://auth-service:8081
      - REDIS_HOST=redis
      - REDIS_PORT=6379
      - LOG_LEVEL=info
    depends_on:
      - postgres
      - redis
      - auth-service
    networks:
      - zride-network

  # AI Matching Service
  ai-matching-service:
    build: ./services/ai-matching-service
    container_name: zride-ai-matching
    ports:
      - "8084:8084"
    environment:
      - DATABASE_URL=postgres://zride_user:zride_password@postgres:5432/zride_ai_matching?sslmode=disable
      - REDIS_URL=redis:6379
      - PORT=8084
      - LOG_LEVEL=info
      - AUTH_SERVICE_URL=http://auth-service:8080
      - USER_SERVICE_URL=http://user-service:8081
      - TRIP_SERVICE_URL=http://trip-service:8083
    depends_on:
      - postgres
      - redis
    networks:
      - zride-network
    restart: unless-stopped

  # API Gateway
  nginx-gateway:
    image: nginx:alpine
    container_name: zride-gateway
    ports:
      - "80:80"
      - "443:443"
    volumes:
      - ./gateway/nginx.conf:/etc/nginx/nginx.conf
      - ./gateway/ssl:/etc/nginx/ssl
    depends_on:
      - auth-service
      - user-service
      - trip-service
      - ai-matching-service
    networks:
      - zride-network

volumes:
  postgres_data:
  user_uploads:

networks:
  zride-network:
    driver: bridge